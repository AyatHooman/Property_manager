@echo off
REM ─── LOCAL SECRETS TEMPLATE ───────────────────────────────────────────────
REM Copy this file to ".secrets.bat" (which is gitignored) and set your real
REM shared access token below. start-server.bat loads it automatically.
set AUTH_TOKEN=change-me
