.env
data/
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/
dist/
build/

# Portable Chrome (181MB - download separately)
chrome-portable/

# Debug / scratch
debug.log
test_*.py
check.py
fix_encoding.py

# Claude Code working files
.claude/

# Stray git bundles
*.bundle

# ── Security: never commit these ────────────────────────────────────────────
# Server logs contain the auth token in request URLs (?token=...) + client IPs
*.log
*.err
server*.log
server*.err
_srv*.log
# Local secrets — real AUTH_TOKEN, any private config. Loaded by start-server.bat.
.secrets.bat
secrets.env
*.local.bat

# Bulk Vicmap easements (~200 MB raw, ~22 MB gzipped, 1.3M features).
# Build locally with `python data/build_easements_vic.py`; not committed.
static/overlays/easements_vic.geojson
static/overlays/easements_vic.geojson.gz

# Bulk Vicmap lots / parcels (~250 MB raw, ~30 MB gzipped, ~2M polygons,
# Greater Melbourne). Build with `python data/build_lots_melb.py`.
static/overlays/lots_melb.geojson
static/overlays/lots_melb.geojson.gz

# Big VicPlan planning-scheme overlays (>20 MB raw). Build with
# data/build_vicplan_overlays_melb.py; gzipped versions are committed below
# the 30 MB threshold instead (.gz files are <10 MB).
static/overlays/vic_emo.geojson
static/overlays/vic_eso.geojson
static/overlays/vic_sbo.geojson

# Domain "for sale" scrape cache — rebuild via the in-app 🔄 Re-scrape button.
data/for_sale.db
